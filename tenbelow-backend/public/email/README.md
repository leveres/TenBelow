Place a PNG logo here named `tenbelow-logo.png` for welcome emails, or set `TENBELOW_EMAIL_LOGO_URL` in Render to a hosted logo URL.

Recommended size: about 264×80 px (displayed at 132px wide in email clients).
